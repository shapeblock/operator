apiVersion: apps.shapeblock.io/v1alpha1
kind: AppBuild
metadata:
  labels:
    app.kubernetes.io/name: op2
    app.kubernetes.io/managed-by: kustomize
  name: appbuild-sample
spec:
  # TODO(user): Add fields here
